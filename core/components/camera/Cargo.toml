# dacho/core/components/camera/Cargo.toml

[package]
name         = "dacho-camera-c"

edition      = "2021"
rust-version = "1.85.0"

authors      = ["mochou-p"]
license      = "MIT OR Apache-2.0"

[dependencies]
glam = { version = "0.29.2", default-features = false, features = ["std"] }

